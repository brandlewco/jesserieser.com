---
label: row-element
hide_body: true
fields:
- name: row_height
  type: select
  default: []
  config:
    required: false
    options:
    - '25'
    - '50'
    - '75'
    - screen
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Row Height
  description: Height of each row. Screen is full height.
  hidden: true
- name: row_alignment
  type: select
  default: between
  config:
    required: false
    options:
    - start
    - center
    - end
    - between
    - around
    source:
      type: simple
      section: 
      file: 
      path: 
  label: Row Alignment
  description: Left to Right alignment
- name: row_order
  type: boolean
  label: Reverse Row Order
  default: false
  description: rows are left to right by default, toggle this to reverse row order.
