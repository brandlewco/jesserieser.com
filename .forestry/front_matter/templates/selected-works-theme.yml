---
label: "⭐️ Featured Image"
hide_body: true
fields:
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
- name: image_mobile
  type: file
  config:
    maxSize: 64
  label: Image Mobile
- name: theme_color
  type: color
  label: Theme Color
  config:
    required: false
    color_format: Hex
